# Implementation guidance

Informative. No production adapter was implemented or tested by the supplied experiment.

Preserve raw token categories and duplicate decoded keys until strict parse and TFJ validation finish. Validate whole-family routing before any output. Hash canonicalisable snapshots before typed validation. Avoid host coercion, prototype properties and platform-dependent datetime arithmetic. Compare object keys by Unicode scalar order.

The verifier establishes authenticity, source authority and inventory completeness outside the evaluator. Construct assertion hashes first (fact minus evidenceRefs), evidence hashes next, snapshot hash next, independent verificationContext last. The trusted context must not be caller-controlled. Fixture contexts are synthetic reproducibility aids. Retain exact artifact/package bytes, snapshot, condition document, context and decisions. Recheck identities before relying on a result.

A possible external host integration, without new fields in governed documents:

1. Establish ordinary Brand Scope and explicit asOf. Obtain exact concrete artifact/action and an immutable input package to inspect. For initial dependency selection the frozen contract allows an input package, not solely a Model Input Package.
2. Evaluate facts and conditions. Retain unresolved outcomes; an empty dependency union does not bypass them.
3. Resolve selected requiresDefined IDs to exact approved manifest elements. Union selected dependencies with static target requirements, dropping none. Submit an ordinary Build Plan to existing compiler mechanisms; do not turn caller assertions into approved brand elements.
4. The compiler still resolves Scope, asOf, subject winners and defined states. Failed targets produce no usable compiled context. Context Assembly uses its validated compiled universe, not sidecar assertions as governed truth. Normal runtime preflight/postflight controls remain in force.
5. If compilation changes the attested input package or generation changes the artifact, obtain new observations/attestations for the new exact bytes and evaluate again before relying on the result. Keep old and new identities separate. Never construct a self-containing snapshot/package hash. No convergence or workflow algorithm is standardized.
6. Retain Task Facts decisions externally, linked by exact artifact/package hashes and existing immutable target/generation identity. Do not add fields to Runtime Decision Records, replace their decisions, or bypass existing release controls.

Future integration checks should cover missing selected IDs, required IDs that lose their subjects, stale generations, zero model calls after failed build, changed artifacts and retention of unresolved conditions. Those checks have not passed in this evidence. No adapter schema, host transport or cryptographic signature protocol is specified here.
